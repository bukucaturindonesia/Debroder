# HISTORICAL POINTER — DO NOT USE AS ACTIVE MASTER STATE

The active master state is located at:

`/DEBRODER_MASTER_STATE.md`

The former append-only document was archived by the P0 governance migration and remains available through Git history and `docs/history/`.
