WORK: DEBRODER Admin Order Detail React #130 Fundamental Fix
BASELINE COMMIT: 74eac91367a20f81f5a24fc341b0e5055ec26947
BASELINE BRANCH LABEL FROM OWNER ZIP: costum-produk-cekout
SCOPE: Admin Order Detail module integrity, renderer selection, timestamp safety, error isolation, and targeted prebuild gate.

IMPORTANT:
- Apply only to a repository checkout matching the baseline commit or review every conflict manually.
- The package contains complete replacement files at repository-relative paths.
- No file must be deleted or renamed manually.
- Migration: NONE / NOT REQUIRED.
- Database and remote Supabase: UNCHANGED.
- Jersey source: UNCHANGED.
- GitHub and Vercel: OWNER-MANAGED; Codex did not commit, push, or deploy.

LOCAL GATE SUMMARY:
- Dependency install: completed dependency extraction, but pnpm reported ignored native build scripts.
- TypeScript: FAIL on pre-existing out-of-scope errors; no changed-file TypeScript error appeared.
- Lint: PASS with 0 errors and 28 pre-existing warnings.
- Targeted command: FAIL on first run because the test invoked Next JSX through Vitest's classic JSX runtime (5/6 passed).
- Corrected targeted test inside full suite: PASS 6/6.
- Full tests: BASELINE FAILURES; 278/284 passed, including React #130 regression 6/6.
- Production build: FAIL after successful compilation, on baseline CartProvider.tsx type error. No attempted-import warning was observed.
- Browser/direct route/action verification: NOT RUN.

OWNER APPLY AND VERIFY:
1. Back up the source and confirm commit 74eac91367a20f81f5a24fc341b0e5055ec26947 or review the delta against the actual deployment source.
2. Copy the package contents into the repository root using Replace/Merge, then review only the listed source changes.
3. Run `pnpm typecheck`, `pnpm lint`, `pnpm vitest run test/admin-order-detail-react130.test.ts`, `pnpm test`, remove `.next`, and run `pnpm build`.
4. Owner commits and pushes only after local gates are acceptable; Codex did not perform either action.
5. Owner deploys Preview and retests direct URL plus refresh on `/admin/orders/[id]` for Ready Stock, Custom, Jersey Ready Stock, Jersey Custom, historical, payment-less, pickup, and external-courier orders. Do not merge if the client-side exception or an action-flow regression remains.

DETAILED OWNER SEQUENCE REQUIRED BY GOVERNANCE:
1. Backup source.
2. Confirm source commit.
3. Apply changed files.
4. Review diff.
5. Run typecheck.
6. Run lint.
7. Run tests.
8. Run clean build.
9. Owner commit.
10. Owner push.
11. Owner deploy.
12. Owner retest Admin Order Detail.

VERIFICATION LIMITS:
- No authenticated production browser was available.
- Production runtime could not be started because the clean build stopped on a baseline type error.
- No production data was changed for testing.
