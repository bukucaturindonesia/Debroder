WORK: DEBRODER Admin Order Detail Pricing Workspace Fundamental Revision
BASELINE: commit 394613b2175188477330165d36a8c2c0f2841f85 (error fix long)
SCOPE: /admin/orders/[id] Stage 3 pricing, its customer quote display, pure pricing validation, local forward migration, and focused regression tests.

COPY/APPLY
1. Extract this ZIP to a temporary folder.
2. Copy all paths into the repository root and choose Replace/Merge.
3. Review the nine source files in FILES_CHANGED.txt.
4. Review and apply supabase/migrations/20260719110000_admin_order_pricing_workspace.sql through the owner-controlled Preview database process BEFORE deploying source that selects custom_pricing_draft columns.
5. Run install, targeted tests, typecheck, lint, full tests, clean build, then controlled Preview browser tests.

FILES TO DELETE MANUALLY: NONE
MIGRATION: PROPOSED LOCALLY — NOT APPLIED
REMOTE SUPABASE: UNCHANGED
GITHUB: UNCHANGED
VERCEL: UNCHANGED

LOCAL GATE LIMITATION
The supplied ZIP did not contain node_modules. The one permitted pnpm install failed before dependency resolution because the runner could not create /root/.local. No install retry or environment workaround was performed. Package-based tests, TypeScript, lint, and Next build therefore did not execute.

LIGHTWEIGHT RESULTS
- Pure pricing domain smoke with Node type stripping: PASS.
- Changed-files diff (ignoring repository baseline CRLF only): PASS.
- Conflict-marker scan: PASS.
- Credential/secret signature scan: PASS.
- Package/prebuild static compatibility check: PASS.

RISKS / OWNER VERIFICATION REQUIRED
- Migration SQL has been statically reviewed but not compiled or applied to a database.
- TypeScript, ESLint, Vitest, clean Next production build, and browser smoke remain owner-run gates.
- Final “send” prepares the quote in the existing customer token/polling flow. No external WhatsApp/email provider was invented.
- Test historical missing-base, Custom, Jersey Custom, Ready Stock, Jersey Ready Stock, pickup, courier, unauthorized/read-only, stale-tab, and double-click cases on controlled Preview data.
